---
to: libs/<%=contextFileName%>/<%=eventStreamFileName%>/src/lib/<%=commandFileName%>/<%=eventStreamFileName%>-<%=eventFileName%>.event.ts
force: true
---
import { Event } from '@ebd-connect/cqrs-framework';

export class <%=TimeLineName%><%=EventName%> implements Event {
  constructor(
<% events[eventsIndex].facts.map( (fact) => {%>    public readonly <%=h.changeCase.camel(fact.name)%> : <%=fact.dataType%>,
<% }) -%>  ) {}
}
